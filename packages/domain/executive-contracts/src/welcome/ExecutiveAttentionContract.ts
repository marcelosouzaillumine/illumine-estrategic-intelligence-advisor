export interface ExecutiveAttentionContract {
  readonly attentionId: string;
  readonly totalIndicatorsEvaluatedCount: number; // 148
  readonly totalEventsCapturedCount: number; // 29
  readonly totalAnalysesCompletedCount: number; // 8
  readonly totalRecommendationsGeneratedCount: number; // 4
  readonly topPrioritiesCount: number; // 2
  readonly singleCriticalDecisionTitle: string; // 1 decisão crítica
}
